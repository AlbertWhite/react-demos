const reducer = (state={list:[], filter:"all"}, action) => {
  switch(action.type){
    case "ADD":
      return
    case "REMOVE":
      return
    case "CHANGE":
      return
    default:
      return state;
  }
}
